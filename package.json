{
	"name": "client-side-painting-variants",
	"version": "1.0.0",
	"description": "Use resource packs to add new types of painting without removing any existing paintings, entirely on the client side!",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "MPL-2.0",
	"dependencies": {
		"@actions/core": "^1.10.1",
		"semver": "^7.6.3"
	},
	"devDependencies": {
		"@types/node": "^22.0.2",
		"@types/semver": "^7.5.8",
		"typescript": "^5.5.4"
	}
}
